{
  "name": "uid-relay",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP wrapper around FAF's faf-uid binary for the FAF mobile web client. One machine, honestly identified as one machine.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "ensure": "node scripts/ensure-faf-uid.js",
    "fingerprint": "node scripts/fingerprint-report.js",
    "test": "node uidtest.js",
    "check": "for f in server.js lib/*.js scripts/*.js uidtest.js; do node --check $f || exit 1; done",
    "preview": "node scripts/machine-preview.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "license": "MIT"
}
